import * as React from "react";

import "./Footer.scss";

// markup
const Footer = (input) => {
  return (
    <footer>
      <div className="footer-button"></div>
    </footer>
  );
};

export default Footer;
